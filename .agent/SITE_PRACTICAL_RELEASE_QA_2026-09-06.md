# Практические входы и редакционные исправления — проверка

Рабочая ветка: `codex/site-practical-first-release`, изолированная копия `/private/tmp/notevskii-site-editorial-fix`, исходная база `ab30fed` поверх `origin/main` (`415396d`). Пользователь разрешил реализацию согласованного плана A/B/C. Исходный checkout и его пользовательский черновик не изменяются.

## Этап A

- Девять разделов Inference Plane отредактированы вместе с контрактом структуры. Старые якоря сохранены отдельными элементами. Даты проверки источников и `reviewedAt` не заменены датой редактуры.
- Подпись записи различает участие и авторство. Ссылки на подтверждённый Telegram-канал обещают чтение канала. Описание учебного расчёта соответствует его контролам. Проект представлен как скилл для проверки сборки запросов.
- Избыточные метки «Проверено» убраны из входа, карты и заголовка reference. Источники, условия применения, предупреждение об устаревании и синтетическая природа кейса сохранены.
- Новый тест роли сначала воспроизвёл неверную подпись, затем прошёл после исправления. Финальный целевой запуск: 6 файлов, 50 тестов. `pnpm lint`, `pnpm typecheck` и `pnpm build` прошли; сборка сохранила 117 страниц, 35 aliases и 54 archives.
- После сборки все прежние IDs найдены: область 9/9, глава 12/12, кейс 5/5. Базовый список: `/private/tmp/notevskii-reference-anchors-before.json`; лог сборки: `/private/tmp/notevskii-release-a-build.log`.
- В браузере проверены глава на 1440 и 390 px, подкаст на 390 px, мобильное меню. На записи видна подпись «Участник», ссылка канала ведёт на `https://t.me/s/sergeinotevskii`. Визуальный проход подтвердил необходимость отдельной правки длинного оглавления в этапе B.
- Независимое редакционное ревью нашло пропущенную метку в общей карте; она исправлена вместе с ожиданием теста. Отдельное кодовое ревью завершилось без замечаний.

## Этап B

- Таблица задач поднята перед первым разделом механизма. Пять веток различают сборку, фактическое чтение, выбор реплики, сведения роутера и перенос KV. В инструкции к скиллу теперь явно разрешены обоснованный итог без правки и указание следующего наблюдения при нехватке данных; установка и пример сохранены.
- Reference-оглавление находится сбоку на широком экране и внутри нативного `details` на узком. Обычный вариант статьи/проекта не изменён. Новый тест раскрываемого варианта сначала падал, после правки прошёл.
- Браузер обнаружил конфликт двух CSS chunks: поздняя `.hidden` перекрывала `.lg:block` в одном слое utilities. Замена на ограниченное `max-lg:hidden` устранила конфликт. После пересборки на 1440 px видимое оглавление шириной 256 px закрепляется на 96 px при переходе к метрикам. На 390 px боковой вариант скрыт, раскрытие работает через Enter, метрики попадают примерно на 112 px от верха. Возврат браузером работает; на 768 px проверены текст, таблицы и учебный кейс. Горизонтального переполнения страницы нет.
- Все 25 прежних редакционных якорей области, главы и кейса сохранены. Один автоматически созданный React ID внутренней подписи калькулятора изменился при перемещении компонента; новая ARIA-связь цела. Этот ID не является редакционным адресом раздела. Переход из таблицы к `#your-project` открывает инструкцию проекта; обычное оглавление проекта сохранено.
- Метод проверен на публичных данных commit `0914f211`: ранняя дата даёт 43 → 254 общих байта; оба разных запроса ожидаемо завершаются с кодом 1. У уже исправленной пары переменный хвост нужен задаче, дальнейшая перестройка не обоснована. Равенство файла самому себе даёт 343 байта и код 0, но ничего не сообщает о чтении KV; следующий шаг — документированные cache-read данные завершённых повторов и маршрут. Это проверка инструкции и текстовых фикстур, без вызова модели. Полный вывод: `.agent/reviews/SITE_FIRST_AUDIT_WALKTHROUGH_2026-09-06.json`.
- Независимые spec/content и code-quality проходы завершились без замечаний. CSS-проблема дополнительно найдена и исправлена в браузере. Финально прошли lint, typecheck, 20 целевых тестов и build (117 страниц, 35 aliases, 54 archives). Лог: `/private/tmp/notevskii-release-b-build.log`.

## Этап C

- В блоге оставлена одна подборка, затем полный каталог авторских текстов; внешний оригинал на Хабре и подписки сохранены. «Все тексты» в шапке ведёт прямо к каталогу. На 390 px начало каталога стало ближе: 1253 px вместо 1919 px; при переходе по ссылке заголовок располагается на 112 px. Это геометрия проверенного варианта, не измерение поведения читателей.
- На главной — один основной разбор, два практических продолжения и три материала с датами из метаданных. Другие темы стоят перед навигационными карточками разделов. Автор, основные входы и внешний оригинал сохранены. Ревью обнаружило дублирование запасного lead при неполном источнике; фильтр по типу/идентификатору и regression test устранили его. Повторное ревью замечаний не нашло.
- Компактное раскрытие серии показывает положение в четырёх самостоятельных статьях и прямые ссылки на остальные. В браузере на 390 px проверены Enter, текущий пункт и переход из третьей статьи сразу в четвёртую. Посторонним статьям серия не добавляется.
- В EN-архиве аудитора изменён только current-section target. Браузер подтверждает self-canonical, `noindex, follow` и переход на `/projects/audit-prompt-caching/`. Остальные aliases/archives сохранены.
- Независимые spec/content и code-quality проверки завершены. Обязательных замечаний не осталось. Старые буквальные assertions о «проверенных» материалах и прежнем регламентном абзаце Inference Plane согласованы с утверждённой редактурой; проверка смысловой границы не зависит от неразрывных пробелов.
- Финальный `pnpm verify` прошёл: lint, typecheck, 25 файлов / 415 тестов / 8 предусмотренных production-only пропусков; 117 страниц; все 46 production export checks; reference audit 6 файлов / 16 переходов; export audit 112 маршрутов / 35 aliases / 54 archives / 23 sitemap URLs. Лог: `/private/tmp/notevskii-site-practical-release-verify.log`.
- Финально просмотрены desktop/mobile Home и Blog, мобильная серия и переход из архива. Ранее в этом же проходе проверены reference и кейс на 390/768/1440 px, запись и меню на 390 px. Снимки: `/private/tmp/notevskii-site-home-desktop.png`, `/private/tmp/notevskii-site-home-mobile.png`. Предпросмотр: `http://127.0.0.1:4176/`.
- Исходный checkout по-прежнему имеет только прежние правки hybrid-reasoners и source.test.ts, `.claude/` и `pnpm-workspace.yaml`; его HEAD/ветка не изменены. Тема кэша в границах этого выпуска закончена; новые материалы, поиск, RSS-модель и переводы остаются отдельной работой.

Изменения сайта пока не опубликованы и не смержены.
