---
entityId: prefix-cache
type: platform-component
locale: ru
slug: prefix-cache
title: "Префиксный кэш"
description: "Как сохранить повторное использование контекста между запросами и выбрать реплику, на которой выгода кэша не исчезнет в очереди."
publicationStatus: published
reviewStatus: reviewed
publishedAt: "2026-07-22"
updatedAt: "2026-09-06"
reviewedAt: "2026-09-05"
reviewCycleDays: 90
section: inference
level: intermediate
status: published
audience:
  - Инженеры AI-платформ
  - Технические лидеры
tags:
  - prefix-cache
  - routing
related:
  - inference-plane
  - agent-session-cache-reuse
published: "2026-07-22"
updated: "2026-09-06"
topics:
  - prefix-cache
  - kv-cache
  - routing
  - inference
relations:
  articleIds:
    - sticky-sessions-vs-prefix-routing
    - what-cache-router-knows
  talkIds:
    - every-token-counts
  projectIds:
    - audit-prompt-caching
sources:
  - title: "vLLM: устройство Automatic Prefix Caching"
    url: "https://docs.vllm.ai/en/stable/design/prefix_caching/"
    verifiedAt: "2026-09-05"
  - title: "vLLM: возможности и ограничения кэширования"
    url: "https://docs.vllm.ai/en/stable/features/automatic_prefix_caching/"
    verifiedAt: "2026-09-05"
  - title: "NVIDIA Dynamo 1.0: маршрутизация с учётом KV-кэша"
    url: "https://docs.nvidia.com/dynamo/v1.0.0/components/router/router-guide"
    verifiedAt: "2026-09-05"
  - title: "SGLang Model Gateway: политики маршрутизации"
    url: "https://docs.sglang.io/docs/advanced_features/sgl_model_gateway"
    verifiedAt: "2026-09-05"
applicability: "Повторяющиеся запросы с длинным общим началом; основной пример — совместимые реплики с локальным KV-кэшем."
limitations: "Учебная модель объясняет выбор маршрута. Она не измеряет GPU, не предсказывает полную задержку и не описывает перенос KV-кэша между серверами."
primaryAreaId: inference-plane
relatedAreaIds: []
decisionQuestions:
  - "Совпадают ли токены после применения шаблона?"
  - "Доступен ли нужный префикс на выбранной реплике?"
  - "Не дороже ли очередь сэкономленной обработки входа?"
  - "Разрешено ли совместное использование кэша?"
metrics:
  - "Повторно использованные входные токены в одном сценарии"
  - "Ожидание, обработка входа и время до первого токена"
  - "Выбранная реплика и возраст сведений о кэше"
  - "Занятость KV-памяти, вытеснения и прерывания"
failureModes:
  - "Изменение токенов в начале запроса"
  - "Попадание на холодную реплику"
  - "Очередь у реплики с популярным префиксом"
  - "Вытеснение нужных блоков до следующего шага"
  - "Устаревшие сведения маршрутизатора"
---

У обсуждения префиксного кэша есть три разных вопроса: изменился ли вход после шаблонизации, попал ли запрос на реплику с нужным префиксом и не съела ли очередь выигрыш от кэша. Здесь разберём, как их отличить и что проверить в каждом случае.

## Проблема

Агент несколько раз отправляет инструкции, описания инструментов и растущую историю. Меняется небольшой хвост, а движок снова тратит время на обработку общего начала. Префиксный кэш (prefix cache) позволяет переиспользовать вычисленное состояние этой части входа.

В пуле из нескольких реплик задача усложняется. Даже полностью совпадающий вход бесполезен для локального кэша сервера B, если нужное состояние осталось только на A. Но закрепить все повторы за A тоже недостаточно: там может накопиться очередь.

Кэш сокращает повторную обработку входа (prefill). Генерацию нового ответа (decode) всё равно нужно выполнить. Если основное время уходит на длинный ответ, сокращение обработки входа может мало изменить полное время запроса. Это ограничение описано в [документации vLLM](https://docs.vllm.ai/en/stable/features/automatic_prefix_caching/).

Выберите продолжение по тому, где теряется повторное использование:

| Задача | Куда перейти |
| --- | --- |
| Меняются инструкции, описания инструментов или схемы | [Проверить кэш в своём проекте](/projects/audit-prompt-caching#your-project), затем воспроизвести [кейс с перестановкой инструментов](/ai-platform/cases/agent-session-cache-reuse) |
| Непонятны правила API или поля usage | Разделить форму входа и [сигналы провайдера](/projects/audit-prompt-caching#provider-usage) |
| Повтор попадает не на ту реплику или упирается в очередь | Сравнить [sticky sessions и маршрутизацию по префиксу](/blog/sticky-sessions-vs-prefix-routing), затем проверить границу в [учебном эксперименте](#experiment) |
| Индекс KV отстаёт от событий или теряется после отказа | Разобрать, [что знает кэш-роутер](/blog/what-cache-router-knows), и подготовить вход для [аудита маршрута](/projects/audit-prompt-caching#routing-audit) |
| Локального KV уже недостаточно | Оценить [экономику KV offload](/blog/kv-offload-economics) до выбора уровня хранения |

## Симптомы

| Наблюдение | Что проверить первым | Чего пока нельзя заключить |
| --- | --- | --- |
| После изменения инструментов стало меньше чтений кэша | Токены и порядок сериализации до первого различия | Что виновата маршрутизация |
| На одной реплике повтор работает, через балансировщик — нестабильно | Выбранную реплику и доступность блоков | Что любой балансировщик обязательно ухудшает результат |
| Попаданий больше, время до ответа хуже | Ожидание у реплик с популярными префиксами | Что кэш нужно отключить |
| После паузы повтор снова требует полной обработки | Вытеснения, перезапуски и смену реплики | Что у всех движков один срок жизни кэша |

Для каждого симптома нужны связанные наблюдения по одному запросу. Графики разных пулов за разные интервалы не складываются в доказательство причины.

## Ментальная модель

Нужны три совпадения: **то же начало**, **совместимое состояние**, **доступное место выполнения**.

В vLLM блок идентифицируется с учётом предшествующего префикса, токенов самого блока и дополнительных признаков — например, адаптера или входного изображения. Повторно используются полные блоки. Одинаковый на вид текст до шаблонизации ещё не гарантирует одинаковый ключ. [Устройство кэша vLLM](https://docs.vllm.ai/en/stable/design/prefix_caching/).

```text
Запрос 1: [инструкции v1][инструменты v3][история][вопрос A]
Запрос 2: [инструкции v1][инструменты v3][история][вопрос B]
           └──────── общее начало ────────────┘

Запрос 3: [новая дата][инструкции v1][инструменты v3][история]…
           ↑ различие до полезного общего контекста
```

Это схема порядка, а не результат работы токенизатора. Длину пригодного для кэша префикса нужно считать после применения настоящего шаблона модели. Сходство смысла здесь не помогает: префиксный кэш не ищет семантически похожий ответ.

Стабильность — договорённость с приложением. Версии инструкций и инструментов должны меняться осознанно. Удалять обязательные ограничения или сохранять недоступный пользователю инструмент ради совпадения префикса нельзя.

## Архитектура

Сначала выбираем сценарий и совместимый пул. Потом решаем, можно ли принять запрос с текущими лимитами и сроком. И только среди подходящих реплик сравниваем локальность и нагрузку.

<FlowDiagram
  title="Три решения на пути запроса"
  steps={[
    { label: "Сценарный роутер", title: "Выбрать возможности", description: "Модель, инструменты, класс обслуживания и допустимый пул." },
    { label: "Допуск и очередь", title: "Проверить ёмкость", description: "Принять, отложить или отклонить по политике и дедлайну." },
    { label: "Кэш-роутер", title: "Выбрать реплику", description: "Учесть доступный префикс и оставшуюся работу." }
  ]}
  caption="Это разделение ответственности. Допуск и выбор реплики могут уточняться совместно; схема не требует трёх отдельных сервисов."
/>

Исправность, совместимость модели и изоляцию данных проверяем до оценки выгоды. Реплика с неподходящей моделью или без нужной изоляции выпадает из списка кандидатов, сколько бы нужных блоков на ней ни лежало. После фильтрации маршрутизатору нужны размер доступного префикса, нагрузка и достаточно свежие сведения о памяти. Если сведения пропали, нужен заранее выбранный режим работы с учётом только нагрузки.

В [Dynamo 1.0](https://docs.nvidia.com/dynamo/v1.0.0/components/router/router-guide) стоимость кандидата учитывает обработку входа и нагрузку генерации, а сведения о блоках могут поступать через события либо оцениваться приближённо. [SGLang Model Gateway](https://docs.sglang.io/docs/advanced_features/sgl_model_gateway) сочетает поиск совпадающего префикса с порогами балансировки. Это конкретные реализации; простая сумма ниже служит только для понимания компромисса.

### Когда холодная реплика выигрывает

Для одного запроса упростим сравнение до двух величин:

```text
T_A = очередь A + оставшаяся обработка входа на A
T_B = очередь B + полная обработка входа на B

Выбираем A, когда T_A < T_B.
При равенстве эта модель не даёт предпочтения.
```

При очереди B в 100 мс, полной обработке в 600 мс и обработке с кэшем в 80 мс граница для очереди A — **620 мс**. Ниже выгоднее A, выше — B. Числа придуманы для эксперимента. Здесь одинаковая производительность реплик, локальный кэш и заданная оценка ожидания; сеть, генерация ответа и влияние нового запроса на остальные не моделируются.

<div id="experiment" className="scroll-mt-28">
  <CacheRoutingLab />
</div>

### Повторить вычисление вне браузера

Из локальной копии [исходного кода сайта](https://github.com/sernote/sernote.github.io) на Node.js 22.18 или новее:

```bash
node --experimental-strip-types scripts/cache-routing-experiment.ts
```

Четыре сценария используют ту же функцию, что и интерактивный пример. В `evidence/v3/cache-routing/result.json` сохранён результат запуска, в соседнем `README.md` — допущения. Команда не обращается к модели или внешнему API. Она проверяет арифметику решения, а не реальную эффективность кэша.

## Метрики

Сначала закрепите модель, токенизатор, шаблон, версию движка, политику маршрутизации и границы пула. Затем соберите данные одного сценария за один интервал.

| Вопрос | Нужные наблюдения |
| --- | --- |
| Было ли что переиспользовать? | Число входных токенов и общий префикс после шаблонизации |
| Произошло ли чтение? | Документированный счётчик повторно использованных токенов или блоков |
| Где выполнили запрос? | Идентификатор реплики, политика и причина выбора |
| Что сравнивал роутер? | Версия, endpoint и фактический вход политики: текст, ключ сессии или другое представление |
| Что выиграл пользователь? | Ожидание, prefill, время до первого токена (TTFT), полное время |
| Какую цену заплатил пул? | Параллельность, занятость KV-памяти, вытеснения и задержки соседних запросов |

Если есть счётчики токенов с согласованным охватом, доля повторного использования равна сумме переиспользованных токенов, делённой на сумму входных. Не усредняйте проценты реплик без весов. Не смешивайте счётчик запросов со счётчиком токенов и не считайте отсутствующее поле нулём.

Название политики не раскрывает её вход, а HTTP 200 не подтверждает завершение потокового ответа. В [разборе кэш-роутера](/blog/what-cache-router-knows) показано, почему chat-путь может сравнивать ключ сессии вместо текста и зачем отдельно проверять завершение SSE на клиенте.

**План проверки на настоящем движке:** повторить один очищенный запрос на той же реплике; отправить его на другую; затем сравнить политики на одинаковом потоке с сохранённым распределением длин, интервалов и параллельности. Для каждой политики отдельно получить холодное и прогретое состояние. Чередовать порядок прогонов, повторять их и сохранять исходные результаты. Не очищать кэш общего рабочего пула ради теста.

Вывод о выгоде требует одновременно данных о чтении кэша и времени выполнения. Интерактивный пример выше этих измерений не заменяет.

## Компромиссы

| Подход | Когда полезен | За что платим |
| --- | --- | --- |
| Распределение по нагрузке | Короткие или редко повторяющиеся входы | Не используем сведения о локальном префиксе |
| Закрепление сессии или префикса за репликой | Повторы часты, нагрузка относительно ровная | Популярные ключи создают очередь; нужна обработка отказа |
| Кэш и нагрузка вместе | Цена повторного prefill заметна и измерима | Индекс кэша, свежесть данных, настройка политики |
| Общий или переносимый KV-кэш | Локальности одной реплики недостаточно | Сеть, хранение, совместимость и отдельная модель стоимости |

При росте числа реплик один и тот же префикс может прогреваться в нескольких местах. Это не обязательно ошибка: дополнительная копия иногда снимает очередь. Выбор зависит от частоты повторов и цены ожидания.

В управляемом API реплика обычно скрыта. Там проверяют контракт конкретного провайдера и возвращаемые поля использования; собственный кэш-роутер перед API не даёт контроля над внутренними GPU-серверами.

## Антипаттерны

- Максимизировать попадания, не ограничивая время ожидания.
- Принимать назначение реплики за доказанное чтение кэша.
- Диагностировать промах только по TTFT: задержка включает больше одной причины.
- Менять модель, права или границу изоляции ради тёплого состояния.
- Считать процент занятой KV-памяти прямым измерением числа вытеснений.
- Использовать один вес скоринга для любых длин входа, ответа и классов обслуживания.
- Выдавать сокращение вычислений за такую же экономию денег: аренда, тариф и полезная загрузка считаются отдельно.

## Чеклист

- [ ] Закреплены модель, шаблон, токенизатор и версия движка.
- [ ] Сравнивается фактический вход, а не исходный объект до сериализации.
- [ ] Проверено, какое представление получает политика на выбранном endpoint и версии роутера.
- [ ] Определено, кому разрешено совместное использование кэша.
- [ ] Реплики фильтруются по совместимости и исправности до оценки выгоды.
- [ ] Есть связь между запросом, выбранной репликой и чтением кэша.
- [ ] Измерены очередь и TTFT по сценариям, а не только среднее по платформе.
- [ ] Завершение потокового ответа проверяется на клиенте отдельно от HTTP-статуса и success роутера.
- [ ] Определены действия при устаревшем индексе, отказе и перегрузке.
- [ ] Выгода проверена на повторяемом потоке, а ухудшение соседних запросов ограничено.

## Связанные главы

[Inference Plane](/ai-platform/areas/inference-plane) описывает более широкую границу исполнения. [Проверка стабильности инструментов](/ai-platform/cases/agent-session-cache-reuse) помогает найти изменение формы запроса до исследования маршрута. [audit-prompt-caching](/projects/audit-prompt-caching#first-audit) даёт инструмент для такого первого прохода.

Авторский вход в тему — [«Тёплый кэш — ещё не быстрый ответ»](/blog/cache-locality-is-a-routing-problem). Три условия повторного использования можно услышать в эфире [«Каждый токен на счету»](/talks/every-token-counts). Здесь используются учебные данные и общая схема, без показателей рабочей платформы.
